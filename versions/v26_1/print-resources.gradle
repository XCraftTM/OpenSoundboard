tasks.register('printMainResources') {
    doLast {
        println("main.resources.srcDirs = " + sourceSets.main.resources.srcDirs)
        println("main.resources.files =")
        sourceSets.main.resources.files.each { println(" - " + it) }
    }
}

