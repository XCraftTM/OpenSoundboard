# Global (non-version) properties. Per-version deps live in versions/<v>/gradle.properties.
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Mod identity
mod_version=0.5.1
maven_group=de.xcrafttm
archives_base_name=opensoundboard

# Modrinth project uploaded to by modrinth.bat
modrinth_project_id=opensoundboard
